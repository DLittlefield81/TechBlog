<div class="post ">
    <article class="card m-2">
        <div class="card-header flex-row justify-space-between">
            <h2>{{post.postTitle}}</h2>
            <div>
                Posted by {{post.user.username}} on {{post.date_created}}
            </div>
        </div>
        <div class="card-body">
            {{post.postContent}}
        </div>
    </article>

    {{#if post.comment}}
    <section class="card m-2">
        <div class="card-header">
            <h3>Comments</h3>
        </div>

        <div class="card-body">
            {{#each post.comment_content}}
            <div>
                <p>
                    {{comment_content}}
                </p>
                <p>
                    &mdash; {{user.username}}, {{date_created}}
                </p>
            </div>
            {{/each}}
        </div>
    </section>
    {{/if}}

    <form id="new-comment-form" class="card m-2">
        <div class="card-header">
            <h3>Add a Comment</h3>
        </div>

        <div class="card-body">
            <input type="hidden" id="post-id" name="post-id" value="{{post.id}}" />

            <label class="form-label" for="comment-content">Comment</label>

            <textarea id="comment-content" name="comment-content" class="form-input"></textarea>

            <button type="submit" class="btn">Submit</button>
        </div>
    </form>
</div>

<script src="/js/comment.js"></script>